package com.synchronus;

public class RestTemplateExample {
    /*
    @Service
    public class OrderService

    {

        private final String url = "https://inventory-service/api/inventory";
        private final RestTemplate restTemplate;

        public String placeOrder(String OrderRequest order, String authToken)
        {
            HttpHeaders headers = new HtttpHeaders;
            headers.setContentType();
            headers.setAccept();
            headers.set(token);

            ResponseEntity<Inventory> response = restTemplate.exchange(url, HTTPMethod.POST, OrderRequest);
            return "order-placed successfully";
        }
*/
}
